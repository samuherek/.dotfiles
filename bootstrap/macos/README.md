To run the bootstrap script without git cloing it, just do 

```sh
curl -fsSL https://raw.githubusercontent.com/samuherek/.dotfiles/multimachine/bootstrap/macos/run.sh | bash
```

(the URL might change based on the git branch)
